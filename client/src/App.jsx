import { BrowserRouter as Router, Routes, Route } from "react-router-dom";
import Layout from "./layouts/main";
const App = () => {
    return (
        <Router>
            <Routes></Routes>
        </Router>
    );
};

export default App;
